test_data_group:
  struct: main.Test
  path:
    - '[short,full,inner,outer].(Inner,Outer)[short,full,outer]'
#    - '[short,full]'
#    - 'Inner[short,full]'
#    - 'Outer[short,full]'
    - '(Outer,Inner)[short,full]'

#    - '[short,full].Inner[short,full]'

#    - '[short,full]'
#    - 'Inner[short,full]'
#    - 'Outer[short,full]'

# /(?<=^\[)[\w|,]+(?=\])/gm || /^\[(\w+(?:,\w+)*)\]/gm || /\[(\w+(?:,\w+)*)\]/gm
# /(?<=.\(|\.|^)[\w|,]+(?=\)|.)/gm || /(?:^|\(|\.|\.\()([\w,]+)/gm
# /(?<=\[)[\w|,]+(?=\]$)/gm || /(?:\[)([\w,]+)(?:\]$)/gm


#  [short,full,inner,outer].(Inner,Outer)[short,full]
#  [short,full,inner,outer].Inner[short,full]
#
#  [short,full]
#  Inner[short,full]
#  Outer[short,full]

# /(?:^|\(|\.|\.\()[\w|,+]/gm
# /(^|\(|\.|\.\((?:,\w+))/gm